{% extends "base.twig" %}

{% block content %}
    {% for post in posts %}
        {% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
        {% if posts|length > loop.index %}
        <hr />
        {% endif %}
    {% endfor %}
{% endblock %}

